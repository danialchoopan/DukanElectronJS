# Plan: Implement Designer Login + Setup Screens

## Goal
Rewrite LockScreen.tsx and SetupWizard.tsx to match the designer's HTML files EXACTLY — both dark and light modes.

## Files to Modify
1. `src/renderer/src/views/LockScreen.tsx` — replace entirely
2. `src/renderer/src/views/SetupWizard.tsx` — replace entirely

## Design System Colors (from designer's code.html)
- Primary: #006194
- Primary Container: #007bb9
- On Primary: #ffffff
- Inverse Primary: #93ccff
- Secondary: #006a61  
- Secondary Fixed: #89f5e7
- Surface: #f8f9ff (light bg)
- Inverse Surface: #233144 (dark bg)
- Inverse On Surface: #eaf1ff
- On Surface: #0d1c2e
- On Surface Variant: #3f4850
- Surface Container Low: #eff4ff
- Outline: #707881
- Outline Variant: #bfc7d2
- Surface Dim: #ccdbf3
- Glass Card Dark: rgba(35, 49, 68, 0.7) backdrop-filter: blur(12px)

## LockScreen.tsx — Exact Design Match

### Dark Mode Layout (from login_screen_desktop_dark/code.html)
- **Full screen**: bg #0d1c2e with gradient blobs
- **12-column grid** (`grid-cols-12 gap-lg max-w-[1200px]`)
- **Right 5 cols** (hidden on mobile): Brand panel
  - Logo image + "تراز نوین" title (use app title instead)
  - Tagline: "نرم‌افزار جامع مالی و حسابداری..."
  - Two glass cards: "امنیت در سطح بانکی" + "گزارش‌های بلادرنگ" (keep as feature descriptions adapted to our app)
- **Left 7 cols**: Glass card login form
  - Header: "خوش آمدید" + subtitle
  - Account selection: 2-col grid of glass-card buttons with person icon, name, last visit text. Selected: border 2px solid #93ccff + glow dot
  - Password field: dark bg, lock icon right, eye toggle left, "ذخیره نشست" checkbox
  - Submit button: "ورود به سیستم" with arrow (← rotated 180)
  - Divider: "یا استفاده از"
  - Biometric: fingerprint + face buttons in 2-col grid
  - Footer: "کاربر جدید هستید؟ درخواست دسترسی"
- **Footer bar**: glass card, copyright + links

### Light Mode Layout (from login_screen_desktop_light/code.html)
- **Full screen**: bg #f8f9ff, subtle blur blobs in corners
- **Centered max-w-[480px]**
- **Brand header**: logo + "تراز نوین" + subtitle
- **White card** with border-outline-variant
  - Header: "خوش آمدید" + "لطفاً حساب کاربری خود را انتخاب کنید"
  - Account grid: 2-col with icon circles (admin_panel_settings / point_of_sale icons). Selected: border-primary + bg surface-container-low
  - Password: underline border-2 style, lock icon, "فراموشی رمز عبور؟" link
  - Security options: fingerprint + key icons with dividers
  - Submit: "ورود به داشبورد" rounded-full primary bg
- **Support footer**: links + copyright

### Shared Functional Requirements
- Keep all existing auth logic (useAuthStore, useCartStore, useSuspendStore, etc.)
- User selection → PIN entry flow
- Error handling, loading states
- PIN entered via existing PinPad component (for the PIN phase)
- For password-style input: use regular password input (not PinPad) to match the designer's single password field design

**IMPORTANT**: The designer's login uses a password field, not a 4-digit PIN pad. The existing PinPad is for the quick PIN entry. For the redesigned login, use a standard password input to match the design.

## SetupWizard.tsx — Exact Design Match

### Dark Mode Layout (from setup_screen_desktop_dark/code.html)
- **Full screen**: bg #0d1c2e (or surface-dim)
- **Top header bar**: logo + "تراز نوین" on right, "راهنما" help on left
- **Centered glass card** (max-w-[560px])
  - Title: "تنظیمات اولیه فروشگاه"
  - Subtitle: "لطفاً مشخصات اصلی کسب‌وکار..."
  - Shop name input: bottom-border style, store icon left (absolute)
  - Two phone fields side-by-side (grid-cols-2): "شماره تماس ثابت" + "شماره همراه", LTR, phone/smartphone icons
  - PIN section: "تعیین رمز عبور سریع (۴ رقمی)" with 4 individual boxes (w-16 h-20, rounded-xl, border-2)
  - Helper text about PIN usage
  - Submit: "ثبت و ورود به داشبورد" with arrow
- **Footer bar**: surface bg, copyright + links

### Light Mode Layout (from setup_screen_desktop_light/code.html)
- **Full screen centered** with split layout (flex-row-reverse)
- **Right half (brand)**: surface-container-low bg, logo + name + tagline, decorative circles
- **Left half (form)**: white bg
  - Title: "راه‌اندازی فروشگاه" + subtitle
  - Shop name: bordered input with store icon right
  - Phone: bordered input with call icon, LTR
  - Address: bordered textarea with location icon
  - PIN: 4 individual boxes (w-14 h-16, rounded-xl, border-2)
  - Helper text
  - Submit: "ایجاد حساب کاربری" rounded-full primary
  - Footer: "قبلاً ثبت‌نام کرده‌اید؟ وارد شوید"

### Shared Functional Requirements  
- Keep all existing setup logic (settings save, user creation, onComplete)
- Language + theme selection (add toggle buttons in the form)
- Shop name, phone, address fields
- 4-digit PIN with RTL auto-advance
- Admin user creation with fixed name

## Implementation Approach

### Step 1: LockScreen.tsx
Rewrite completely. Key structural differences from current:
- Dark mode needs the 12-col grid with brand panel + glass card (currently single-col)
- Switch from PinPad to password input field (matching designer's single input)
- Add glass-card CSS class via inline styles
- Add decorative gradient background elements
- Add "ذخیره نشست" checkbox (visual only, no logic needed)
- Account cards need icon + text layout matching designer (horizontal not vertical)

### Step 2: SetupWizard.tsx  
Rewrite completely. Key differences:
- Dark mode: header bar + centered glass card (currently no header)
- Light mode: split layout with brand panel (currently has it but needs refinement)
- Bottom-border input style for dark mode (currently uses filled inputs)
- 4 individual PIN boxes (currently has this, needs size/style matching)
- Two phone fields in dark mode (currently one)

## Verification
1. `npx tsc --noEmit` → 0 errors
2. `npx vite build` → success
3. Run app, verify dark mode login matches designer screenshot
4. Run app, verify light mode login matches designer screenshot
5. Run app, verify dark mode setup matches designer screenshot  
6. Run app, verify light mode setup matches designer screenshot
7. Test: select user → enter password → login works
8. Test: complete setup → creates admin user → redirects to login
