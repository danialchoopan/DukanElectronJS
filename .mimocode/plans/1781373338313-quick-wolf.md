# Plan: Fix Search, Auto-Barcode, Success Messages, Database

## Bug 1: Search not working (BarcodeInput clears on failed lookup)
**File:** `src/renderer/src/components/BarcodeInput.tsx`
- Line 57: Enter key calls `onBarcodeScanned` then clears input — user sees their text vanish
- Fix: Don't clear input on failed barcode lookup. Only clear on successful product selection from dropdown.

## Bug 2: Auto-barcode collision
**File:** `src/main/ipc/handlers.ts`
- Current: `SELECT MAX(id)` → collides with soft-deleted product barcodes
- Fix: Use `SELECT MAX(CAST(SUBSTR(barcode, 5) AS INTEGER))` to get max barcode number, not product ID

## Bug 3: Success message on failure
**File:** `src/renderer/src/views/AddProduct.tsx`
- The `showNotif` IS guarded by `if (r.success)`, BUT the form closes and resets even on failure
- Fix: Only close form and show notification on success. On failure, show error message and keep form open.

## Bug 4: Database schema
- Add unique constraint on `suspended_invoices(userId, slotIndex)`
- No other schema changes needed

## Files to modify
1. `src/renderer/src/components/BarcodeInput.tsx` — fix search clearing
2. `src/main/ipc/handlers.ts` — fix auto-barcode generation
3. `src/renderer/src/views/AddProduct.tsx` — fix success/error handling
4. `src/main/database/connection.ts` — add unique constraint (optional)

## Verification
1. Add product with empty barcode → should auto-generate PRD-XXXXXX
2. Search in POS → type and see results without input clearing
3. Add product with missing fields → error message shown, form stays open
4. Add product successfully → green notification, form closes
